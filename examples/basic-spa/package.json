{
  "name": "xmcloud-foundation",
  "private": true,
  "version": "22.7.0",
  "description": "This repository contains the Starter Kits for Sitecore XM Cloud Development.",
  "scripts": {
    "install-build:angular": "npm run install:all && npm run build:angular",
    "install:all": "cd angular && npm install && cd ../proxy && npm install && cd ..",
    "build:angular": "cd angular && npm run build && cd ..",
    "start:angular": "cd proxy && npm run start"
  },
  "license": "ISC",
  "packageManager": "pnpm@9.11.0",
  "engines": {
    "node": ">=22.0.0"
  }
}
